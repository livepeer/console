diff --git a/dist/index.cjs b/dist/index.cjs
index ad4d1bfc4a8a21764c9f598ee61f914f80900e39..d13e17c6f72fb90346b66000f5efcc1cbc95435f 100644
--- a/dist/index.cjs
+++ b/dist/index.cjs
@@ -1123,6 +1123,16 @@ async function getRunnerPayment(options) {
     gatewayRequestId: options.gatewayRequestId,
     attributionSource: options.attributionSource
   });
+  // Persist the manifest before payment; callback failures must never trigger paid failover.
+  const observe = async (phase) => {
+    if (!options.onPayment) return;
+    try {
+      await options.onPayment({ manifestId: options.challenge.manifestId, phase });
+    } catch {
+      throw new Error("payment_manifest_persistence_failed");
+    }
+  };
+  await observe("prepared");
   const payment = await session.getPayment();
   if (!payment.payment) {
     throw new LivepeerGatewayError("Live runner payment response missing payment");
@@ -1130,6 +1140,7 @@ async function getRunnerPayment(options) {
   if (!payment.segCreds) {
     throw new LivepeerGatewayError("Live runner payment response missing segCreds");
   }
+  await observe("accepted");
   return { session, payment };
 }
 async function resolveChallengePayment(options) {
@@ -1141,7 +1152,8 @@ async function resolveChallengePayment(options) {
     maxPrice: options.maxPrice,
     app: options.app,
     gatewayRequestId: options.gatewayRequestId,
-    attributionSource: options.attributionSource
+    attributionSource: options.attributionSource,
+    onPayment: options.onPayment
   });
   options.requestHeaders["Livepeer-Payment"] = paid.payment.payment;
   options.requestHeaders["Livepeer-Segment"] = paid.payment.segCreds ?? "";
@@ -1206,7 +1218,8 @@ async function attemptPaidCall(input) {
         app: input.runner?.app ?? null,
         requestHeaders,
         gatewayRequestId: input.gatewayRequestId,
-        attributionSource: input.attributionSource
+        attributionSource: input.attributionSource,
+        onPayment: input.onPayment
       });
       paymentSession = paid.session;
       sessionId = paid.sessionId;
@@ -1266,7 +1279,8 @@ async function callRunner(options) {
       insecureTls: options.insecureTls !== false,
       method: options.method ?? "POST",
       gatewayRequestId: options.gatewayRequestId ?? null,
-      attributionSource: options.attributionSource ?? null
+      attributionSource: options.attributionSource ?? null,
+      onPayment: options.onPayment
     });
     if (outcome.kind === "success") return outcome.result;
     challenge = outcome.challenge;
@@ -1670,7 +1684,8 @@ async function reserveSession(options) {
     timeoutMs: options.timeoutMs,
     insecureTls: options.insecureTls,
     gatewayRequestId: options.gatewayRequestId,
-    attributionSource: options.attributionSource
+    attributionSource: options.attributionSource,
+    onPayment: options.onPayment
   });
   const sessionId = stringField(result.data, "session_id");
   const appUrl = stringField(result.data, "app_url");
@@ -1871,7 +1886,8 @@ function createGateway(config) {
       timeoutMs,
       insecureTls,
       gatewayRequestId,
-      attributionSource: config.attributionSource ?? null
+      attributionSource: config.attributionSource ?? null,
+      onPayment: req.onPayment
     });
     return { runnerUrl, data: result.data, providerRequestId: result.providerRequestId };
   }
@@ -1885,7 +1901,8 @@ function createGateway(config) {
       timeoutMs,
       insecureTls,
       gatewayRequestId,
-      attributionSource: config.attributionSource ?? null
+      attributionSource: config.attributionSource ?? null,
+      onPayment: req.onPayment
     });
     try {
       const result = await callSession(session, {
@@ -2010,7 +2027,8 @@ function createGateway(config) {
               insecureTls,
               startFunding: req.startFunding,
               gatewayRequestId,
-              attributionSource: config.attributionSource ?? null
+              attributionSource: config.attributionSource ?? null,
+              onPayment: req.onPayment
             });
           } catch (e) {
             lastError = e;
diff --git a/dist/index.d.ts b/dist/index.d.ts
index e11cf5c7a1da500c1ca05024a39715d2e5f35764..3b196c47fe84fc45d800813b724bdd7d31341412 100644
--- a/dist/index.d.ts
+++ b/dist/index.d.ts
@@ -234,6 +234,8 @@ interface LiveRunnerCallResult {
     billableUnits: number | null;
 }
 interface CallRunnerOptions {
+    /** Awaited before payment and after acceptance; failures abort without paid failover. */
+    onPayment?: (payment: { manifestId: string; phase: "prepared" | "accepted" }) => Promise<void>;
     runnerUrl?: string;
     runner?: LiveRunnerInstance | null;
     payload?: Record<string, unknown>;
@@ -314,6 +316,8 @@ interface RunnerSession {
     stopPayments(): Promise<void>;
 }
 interface ReserveSessionOptions {
+    /** Awaited before payment and after acceptance; failures abort without paid failover. */
+    onPayment?: (payment: { manifestId: string; phase: "prepared" | "accepted" }) => Promise<void>;
     runner: LiveRunnerInstance;
     payload?: Record<string, unknown>;
     signerUrl: string;
@@ -378,6 +382,8 @@ interface GatewayConfig {
     attributionSource?: string;
 }
 interface InferenceRequest {
+    /** Awaited before payment and after acceptance; failures abort without paid failover. */
+    onPayment?: (payment: { manifestId: string; phase: "prepared" | "accepted" }) => Promise<void>;
     capability: string;
     params?: Record<string, unknown>;
     prompt?: string;
@@ -421,6 +427,8 @@ interface InferenceResult {
     billableUnits: number | null;
 }
 interface ReserveSessionRequest {
+    /** Awaited before payment and after acceptance; failures abort without paid failover. */
+    onPayment?: (payment: { manifestId: string; phase: "prepared" | "accepted" }) => Promise<void>;
     capability: string;
     params?: Record<string, unknown>;
     app?: string;
diff --git a/dist/index.js b/dist/index.js
index ec91cfe15b2c7e01af088af1458e0c8a733c5791..4f458b279cc9e4ecdbc1a2c421aee0c6e1c585c3 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -1049,6 +1049,16 @@ async function getRunnerPayment(options) {
     gatewayRequestId: options.gatewayRequestId,
     attributionSource: options.attributionSource
   });
+  // Persist the manifest before payment; callback failures must never trigger paid failover.
+  const observe = async (phase) => {
+    if (!options.onPayment) return;
+    try {
+      await options.onPayment({ manifestId: options.challenge.manifestId, phase });
+    } catch {
+      throw new Error("payment_manifest_persistence_failed");
+    }
+  };
+  await observe("prepared");
   const payment = await session.getPayment();
   if (!payment.payment) {
     throw new LivepeerGatewayError("Live runner payment response missing payment");
@@ -1056,6 +1066,7 @@ async function getRunnerPayment(options) {
   if (!payment.segCreds) {
     throw new LivepeerGatewayError("Live runner payment response missing segCreds");
   }
+  await observe("accepted");
   return { session, payment };
 }
 async function resolveChallengePayment(options) {
@@ -1067,7 +1078,8 @@ async function resolveChallengePayment(options) {
     maxPrice: options.maxPrice,
     app: options.app,
     gatewayRequestId: options.gatewayRequestId,
-    attributionSource: options.attributionSource
+    attributionSource: options.attributionSource,
+    onPayment: options.onPayment
   });
   options.requestHeaders["Livepeer-Payment"] = paid.payment.payment;
   options.requestHeaders["Livepeer-Segment"] = paid.payment.segCreds ?? "";
@@ -1132,7 +1144,8 @@ async function attemptPaidCall(input) {
         app: input.runner?.app ?? null,
         requestHeaders,
         gatewayRequestId: input.gatewayRequestId,
-        attributionSource: input.attributionSource
+        attributionSource: input.attributionSource,
+        onPayment: input.onPayment
       });
       paymentSession = paid.session;
       sessionId = paid.sessionId;
@@ -1192,7 +1205,8 @@ async function callRunner(options) {
       insecureTls: options.insecureTls !== false,
       method: options.method ?? "POST",
       gatewayRequestId: options.gatewayRequestId ?? null,
-      attributionSource: options.attributionSource ?? null
+      attributionSource: options.attributionSource ?? null,
+      onPayment: options.onPayment
     });
     if (outcome.kind === "success") return outcome.result;
     challenge = outcome.challenge;
@@ -1596,7 +1610,8 @@ async function reserveSession(options) {
     timeoutMs: options.timeoutMs,
     insecureTls: options.insecureTls,
     gatewayRequestId: options.gatewayRequestId,
-    attributionSource: options.attributionSource
+    attributionSource: options.attributionSource,
+    onPayment: options.onPayment
   });
   const sessionId = stringField(result.data, "session_id");
   const appUrl = stringField(result.data, "app_url");
@@ -1797,7 +1812,8 @@ function createGateway(config) {
       timeoutMs,
       insecureTls,
       gatewayRequestId,
-      attributionSource: config.attributionSource ?? null
+      attributionSource: config.attributionSource ?? null,
+      onPayment: req.onPayment
     });
     return { runnerUrl, data: result.data, providerRequestId: result.providerRequestId };
   }
@@ -1811,7 +1827,8 @@ function createGateway(config) {
       timeoutMs,
       insecureTls,
       gatewayRequestId,
-      attributionSource: config.attributionSource ?? null
+      attributionSource: config.attributionSource ?? null,
+      onPayment: req.onPayment
     });
     try {
       const result = await callSession(session, {
@@ -1936,7 +1953,8 @@ function createGateway(config) {
               insecureTls,
               startFunding: req.startFunding,
               gatewayRequestId,
-              attributionSource: config.attributionSource ?? null
+              attributionSource: config.attributionSource ?? null,
+              onPayment: req.onPayment
             });
           } catch (e) {
             lastError = e;
